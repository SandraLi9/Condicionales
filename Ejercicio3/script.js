//3. Crear un programa que determine si un número introducido en 
//un Prompt es par o no, la respuesta será mostrada en un Alert.

let SolicitarNumero = prompt("Ingresa un numero")

if (SolicitarNumero % 2 == 0) {
   alert("Numero Par");
}
else {
   alert("Numero Impar")
}