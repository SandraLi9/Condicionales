//2. Solicitar al usuario un número, y determinar si es divisible entre dos o no.Mostrando al usuario un mensaje 
//de “x número es divisible entre 2” o “x núm no es divisible entre 2”.

let SolicitarNumero = prompt("Ingresa un numero")

if (SolicitarNumero % 2 == 0) {
   alert("Numero es divisible entre 2");
}
else if (SolicitarNumero) {
   alert("Numero no es divisible entre 2")
}
